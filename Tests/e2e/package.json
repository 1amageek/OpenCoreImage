{
  "name": "opencoreimage-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser end-to-end tests for the OpenCoreImage smoke-test WASM app. Validates CIColor / CIVector / CIImage API surface under the wasm32 target via swift-testing running in headless Chromium.",
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "serve": "node server.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "swift-wasm-testing": "github:1amageek/swift-wasm-testing"
  }
}
